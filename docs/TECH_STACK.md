# Tech Stack

Current high-level stack (subject to change):

- Java 21
- Spring Boot
- Maven
- LLM APIs (pluggable)
- Local-first development

The application currently runs via:

```
export JAVA_HOME=$(/usr/libexec/java_home -v 21) && mvn spring-boot:run
```
